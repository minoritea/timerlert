# timerlert

```
NAME:
   timerlert - A simple timer command with alerts for MacOS

USAGE:
   timerlert [global options] message

GLOBAL OPTIONS:
   --title value, -t value  set notification title
   --wait value, -w value   waiting time until the alert is displayed
   --help, -h               show help
```
